const uglify = require('uglify-js');
const fs = require('fs');

const options = { compress: true };
const file = fs.readFileSync('dist/simulation.js').toString();

const result = uglify.minify(file, options);
fs.writeFileSync('dist/msimulation.min.js', result.code);
